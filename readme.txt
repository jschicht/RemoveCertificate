Simpel application that will remove the authenticode digital signature (certificate) from an executable. Nice to have if you re-sign executables.

Uses the function ImageRemoveCertificate in Imagehlp.dll.